// Change this file after the approved video or extracted SCORM package is supplied.
// mode: "pending" | "video" | "scorm". Never use a ZIP file as a player source.
window.REDELIUS_DEMO = {
  mode: 'scorm',
  src: 'demo/index.html',
  captions: '',
  // For SCORM: relative launch HTML path from imsmanifest.xml, after extraction.
  // Confirm standalone preview support; actual tracking must be tested in Educere.
};
